
class AudioTracker(object):
    def __init__(self, audioText):
        # self.audio = audio
        ### if it is a search
        # creating a search list to detect
        searchVal = ['search']
        
        if searchVal in audioText:
            searchIndex = audioText.index('search')
            if 
            print("yes")
        else:
            print("no")

        